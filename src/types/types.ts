export interface RegisterData {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  password: string;
}

export interface PracticeItemData {
  title: string;
  category: string;
  duration: number;
  userId: string;
}
